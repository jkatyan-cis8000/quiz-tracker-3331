# Config Layer

Configuration loading and application settings.

## Purpose
- Load configuration from files
- Provide default values
- Environment-specific settings

## Rules
- May import from: types, config
- Parse config files into type definitions
