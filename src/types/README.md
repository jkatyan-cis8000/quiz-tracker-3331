# Types Layer

Pure type definitions with no business logic.

## Purpose
Define all data models used throughout the application:
- Question types and structure
- Quiz configuration
- Quiz results and scoring

## Rules
- No imports of internal modules (types only import from themselves)
- No business logic, only data structures
