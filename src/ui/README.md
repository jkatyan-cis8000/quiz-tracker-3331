# UI Layer

User-facing interfaces (CLI, web, GUI).

## Purpose
- CLI interaction for quiz
- Display questions
- Get user input
- Show results

## Rules
- May import from: types, config, service, runtime, providers, ui
- No business logic - just presentation
